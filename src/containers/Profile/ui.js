import { styled } from '@mui/system'
import { Box, Switch, Tabs, TextField, Typography } from '@mui/material'

export const UIImage = styled('img')(({}) => ({
  width: '104px',
  height: '104px',
  objectFit: 'cover',
  borderRadius: '50%',
}))
